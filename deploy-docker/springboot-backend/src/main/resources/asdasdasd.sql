-- -- populate database for Role table
-- insert into roles (id, code, name) 
-- values (1, 'ROLE_USER', 'User'),
--        (2, 'ROLE_ADMIN', 'Admin');

-- -- populate database for User table (based on User.java entity)
-- insert into users (id, name, email, password)
-- values (1, 'Jack', 'Jack@gmail.com', '1221');

-- -- populate database for User_Role table
-- insert into user_roles (user_id, role_id)
-- values (1, 1);
    